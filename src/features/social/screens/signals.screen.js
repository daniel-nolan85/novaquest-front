import { View } from 'react-native';
import { Text } from '../../../components/typography/text.component';

export const SignalsScreen = () => {
  return (
    <View>
      <Text variant='title'>Signals Screen</Text>
    </View>
  );
};
